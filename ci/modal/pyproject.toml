[project]
name = "kryo-modal"
version = "0.1.0"
description = "Modal deployment for cold start benchmarks"
requires-python = ">=3.11"
dependencies = [
    "modal>=1.3.0",
    "numpy>=2.4.0",
]

[tool.uv]
dev-dependencies = []
